# Raspberry Pi Master Server Dependencies
# Install with: pip install -r requirements.txt

# Web server + WebSocket
flask>=3.0
flask-socketio>=5.3
eventlet>=0.35

# I2C sensor support (Adafruit CircuitPython)
adafruit-blinka>=8.0
adafruit-circuitpython-apds9960>=3.0
adafruit-circuitpython-tca9548a>=0.7

# RFID reader (Pi 5-compatible fork using gpiozero instead of RPi.GPIO)
# Install separately: pip install git+https://github.com/Dennis-89/MFRC522-python-SimpleMFRC522.git

# GPIO — installed as system packages (sudo apt install python3-lgpio)
# lgpio (Pi 5) and RPi.GPIO (Pi 4) are accessed via --system-site-packages venv
